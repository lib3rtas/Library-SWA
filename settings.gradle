rootProject.name = 'library_swa'

include 'app'       // the spring startet app
include 'db'        // database access layer
include 'domain'    // domain classes (models, services, interfaces, ...)
include 'spring'    // spring configuration classes
include 'web'       // web controller